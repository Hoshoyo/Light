string struct {
    capacity : u64;
    length   : u64;
    data     : ^u8;
}

main : () -> s32 {
    //b := #size_of(string);
    c := #size_of #type_of(main);
    d : #type_of(main);
    return 0;
}