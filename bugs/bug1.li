GetStdHandle : (std_handle : s32) -> ^void #extern("Kernel32.dll");
main : () -> s32 {
    while true {
        //while false {
        //    //a := 0;
        //}
    }
    a := GetStdHandle(-11);
    return 0;
}

// return *(val -> s32^) -> u64; // CRASH
// GL_POLYGON : GLenum : 0x0009; // CRASH

// Global strings crashes in the vm

// /* /* */ crashes