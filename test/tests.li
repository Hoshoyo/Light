#import "../modules/windows/print.li"

main : () -> s32 {	
	b : u64 = 0x123456789abcd;
	a : s64 = 0x123456789abcd; // 320255973501901
	f : hello;

	print("% % % % % % %\n", 11, a, 33 -> u32, 'c', b, true, 3.14, &a);
	print("$ $ $ $ $ $ $\n", 11, a, 33 -> u32, 'c', b, true, 3.14, &a);
	print("main is a $\n", foo);
	print("Alias is % bytes in size\n", #size_of hello);

	return 0;
}

hello s32;

foo : (a : s32) -> s32 {
	return 0;
}