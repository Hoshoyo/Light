//ExitProcess :: (r : u32) -> u32 #foreign("kernel32.lib");

main :: () -> s32 {
	d := 0;
	
	for i := 0; i < 5; i += 1 {
		d = factorial(i);
	}
	
	return [s32]d;
}

factorial :: (v : s64) -> s64 {
	if v <= 1 return 1;
	return v * factorial(v - 1);
}