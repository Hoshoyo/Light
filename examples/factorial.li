ExitProcess :: (r : u32) -> u32 #foreign("kernel32.lib");

factorial :: (v : u32) -> u32 {
	if v == 1 return 1;
	return v * factorial(v - 1);
}

main :: () -> s32 {
	a : u32;
	a = factorial(5);
	a = ExitProcess(a);
	return 0;
}