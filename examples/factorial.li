//ExitProcess :: (r : u32) -> u32 #foreign("kernel32.lib");

#if WINDOWS
factorial :: (n : s32) -> s32 {
	if n <= 1
		return 1;

	return n * factorial(n-1);
}
#endif

main :: () -> s32 {
	return factorial(5);
}
