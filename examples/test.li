ExitProcess  :: (exit_code : s32) -> s32 #foreign("kernel32");
print_string :: (length : s64, data : ^s8) -> void #foreign("msvcrt");
print_s64 :: (v : s64) -> s64 #foreign("msvcrt");
print_double :: (v : r64) -> void #foreign("msvcrt");
main :: () {
	/*
		d = 0.0;
		d *= d;
		d /= d;
		d += d;
		d -= d;
		d + d;
		d - d;
		d / d;
		d * d;
		d < d;
		d > d;
		d <= d;
		d >= d;
		d == d;
		d != d;
	*/
	
	a : r64 = 2.0 - 3.0;
	if a > 0.0
		print_double(a);
	else
		print_double(99.9);
	
	ExitProcess(0);
	return;
}